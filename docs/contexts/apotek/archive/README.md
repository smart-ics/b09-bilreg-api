# Archived Apotek artifacts

```yaml
Artifact-Type: ArchiveIndex
Status: Archived
Normative-Level: Historical
```

These reports are retained for traceability only. They must not be retrieved in default work mode.

Decisions they produced now live in Active artifacts (`apotek-domain.md`, workflow, SOP, screen/aggregate, persistence, `adr/`).

Do not open this folder unless reconstructing why a decision was made.
